# CapEgo working agreements

## Scope and decisions

- 项目品牌名称为 **CapEgo**，仓库及命令标识使用 `capego`，目标仓库为 `Auromix/capego`。
- 2026-09-24 用户授权开始实现、在 macOS 开发并用模拟数据逐模块测试，达到可复现阶段提交 GitHub。按已确认产品边界推进；实现所需的可逆技术选择记录到设计中，不把模拟测试写成实体设备、GPU 或真实模型验收。
- 每轮优先澄清一个影响产品边界的问题。优先讨论使用者、任务和交付结果，暂不确定软硬件选型。
- 以用户最新确认的范围为准。不能把助手建议、外部项目做法或客户候选指标自动转成已确认设计。
- 已确认的产品定义以 `design/product/scope.md` 为准，使用流程见 `design/product/workflow.md`。首版面向开源开发者和研究者，提供个人或单个实验台可复现、可扩展的参考方案。
- 首版同一台 Ubuntu 24.04 PC 承担接收存储、后处理和工作台，完整后处理以本地 GPU 为参考配置；具体硬件未选定。总体职责与采集规则见 `design/system/overview.md`。
- 开始新录制前须确认 PC 已连接且能够接收保存；条件不满足时不开始采集并提示原因。录制途中断连仍按缓存与补传规则处理，不将这一行为扩展为离线起录。
- 主流程是边采集边持续通过局域网向 PC 传输并落盘，PC 是原始数据正式保存位置。设备缓存仅暂存未获 PC 可靠保存确认的数据，随确认持续释放，并在断连恢复后补传；不能改成先在设备保存整次录制、结束后再上传。
- 正常运行按临时缓存不耗尽设计；缓存满属于异常，直接结束本次录制，保留已有数据和原因，不覆盖未保存数据。因缓存耗尽结束后，恢复连接只自动补传，不自动续录或另开录制。
- 一次录制对应一个文件夹，分块存储、统一读取；结束录制不停止已有数据传输。只有录制结束且全部数据落盘并校验完整，才能选入后处理，不得恢复“已封块即可提前处理”的旧建议。
- 采集接收与后处理独立运行，业务上不互斥。批次内各采集独立处理、失败可单独重试；重新处理保留旧版本。
- 后处理必须由用户选择批次并主动启动；启动后自动处理和标注，人工以问题检查、抽查及少量修正为主。新数据到达不触发后处理。
- 自动标注、检查、准入和版本规则见 `design/data/processing.md`。先固定数据集内容版本，再按训练目标导出；后续标注修改不能改变已有数据集版本。
- 首次准备软件、依赖和模型允许联网；准备完成后的全流程无互联网依赖，但采集与接收 PC 之间仍需要局域网连接。
- 数据集导出以实际接入开源 WAM 训练为目标，按具名仓库、版本和验证证据声明支持，不把更换文件容器描述为训练兼容。
- 首版训练接入参考 RTX 5880 48GB 级别单卡，先打通一个目标；具体预算与验证范围见 `design/data/training-integration.md`。该预算不自动选定后处理 PC 的具体显卡，EgoWAM 仍是待验证候选。
- 当前训练接入只考虑 ego 数据的实际可用性：目标 loader 读取并参与有效的最小训练步骤。不规划机器人数据、混合训练比例或模型效果复现，不以这些问题阻塞 ego 设计；数据正确性、质量和来源要求继续保留。
- Ego 数据分类和时间原则见 `design/data/ego-data.md`：保留原始采样与时间戳，通过单次采集统一时间轴关联，跨块不重置；接收和补传时间不替代采样时间，导出对齐需标记缺失及插值。
- 双手三维数据支持相机坐标与单次采集局部参考系，保留相机运动轨迹及有效性信息。具体坐标轴、单位、姿态表示和算法待设计，不将局部参考系写成已实现的无漂移全球定位。

## Documentation

- `design/` 用于产品设计、系统架构、软硬件设计、数据设计及验证方法。
- Markdown 是权威来源。具体技术设计形成前，不为填满目录而添加未经确认的架构、API、配置或选型。
- 区分已确认范围、候选方案、待讨论事项、实现状态、测试证据和验收结果。
- 一项定义维护在一个位置，其他页面链接引用。修改后检查相对链接、术语、单位和状态的一致性。
- 设计文档以中文为主，根 README 提供简短英文定位，关键字段保留准确英文术语。

## Data and publication

- 不提交真实采集数据、客户私有资料、个人信息、密钥、凭据或本地生成的数据集。
- 不将缺失数据写成有效观测；不将算法估计的手部轨迹直接描述为机器人动作真值。
- 开源方向已确定，具体许可证尚待讨论。在正式配置许可证前，不声称仓库已有某种开源授权。

## Local previews

- 预览或检查本地 HTML 时，通过绑定 `127.0.0.1` 的临时 HTTP 服务访问，优先使用内置浏览器。
- 不使用 `file://` 做浏览器自动化或视觉检查。
- 任务结束后关闭本次启动的预览服务，除非用户要求保留。
